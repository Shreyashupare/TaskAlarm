# TaskAlarm

`TaskAlarm` is a simple alarm app that requires short tasks before stop is unlocked.

## Current state

- Project setup and documentation phase.
- Product docs are in `docs/`.
- Decisions and progress logs are in `logs/`.
- AI assistant rules are in `meta/ai/`.
